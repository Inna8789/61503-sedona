

(function() {

	var elements = document.querySelectorAll(".fieldset__triple");

	for(var i = 0; i,elements.length; i++) {

		initNumberField(elements[i]);
	}

	function initNumberField(parent) {

		var input = parent.guerySelector("input");

		var minus = parent.guerySelector(".fieldset__field--btn_minus");

		var plus = parent.guerySelector(".fieldset__field--btn_minus");

		minus.addEventListener("click", function() {
			changeNumber(false);
		});

		plus.addEventListener("click", function() { 
			changeNumber(true);
		});

		function changeNumber(operation){

			var value + Number(input.value);

			if (isNan(value)) {
				value = 0;
			}

			if (operation) {
				input.value = value + 1;
			} else {
				input.value = value - 1;
			}

		}
	}

})();